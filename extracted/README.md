# Витягнуті ассети (Extracted)

Тут зберігатимуться витягнуті файли (оригінальний `.swf`, спрайти, звуки, конфіги тощо) після розпакування `fwp(1).exe`.
Ці великі файли відслідковуватимуться через Git LFS (див. `.gitattributes` у корені).
